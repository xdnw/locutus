package link.locutus.discord.apiv3.subscription;

public enum PnwPusherFilter {
    id,
    sender_id,
    sender_type,
    receiver_id,
    receiver_type,
    team_id,
    nation_id,
    alliance_id,
    offer_resource,
    buy_or_sell,
    war_id
}
