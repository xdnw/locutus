package link.locutus.discord.db.entities;

public enum BankNote {
    // "#grant", "#deposit", "#trade", "#ignore", "#tax", "#warchest", "#account"
    GRANT,
    DEPOSIT,
    TRADE,
    IGNORE,
    TAX,
    WARCHEST,
    ACCOUNT
}
